export const GooglePubSubConfig = {
    history_backup_max_length: 20,
    //  * If we want to quickly disable Google PubSub, we can do it by setting GooglePubSubConfig.enabled = false
    enabled: true,
};
